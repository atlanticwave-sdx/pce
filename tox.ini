[tox]
env_list =
    py3

[testenv]
description = run the tests with pytest
package = wheel
wheel_build_env = .pkg
deps =
    pytest>=6
    coverage>=7
extras = 
    pygraphviz    
commands =
    coverage run -m pytest {tty:--color=yes} {posargs}
    coverage report
