[tox]
env_list =
    py311
minversion = 4.4.4

[testenv]
description = run the tests with pytest
package = wheel
wheel_build_env = .pkg
deps =
    pytest>=6
    coverage>=7
commands =
    coverage run -m pytest {tty:--color=yes} {posargs}
    coverage report
